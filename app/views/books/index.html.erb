<h1>投稿一覧</h1>

<% @books.each do |book| %>

  <p>Title:</p>
  <span><%= book.title %></span>

  <p>Body:</p>
  <span><%= book.body %></span>

  <span>
    <%= link_to book.title, book_path(book.id) %>
  </span>
<!--リンク名をshowにする-->


<% end %>